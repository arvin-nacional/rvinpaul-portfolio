// @use-gesture/react re-exports this type-only entrypoint at runtime, but the
// @use-gesture/core 10.3.1 package omits its compiled files. Type exports have
// no runtime value, so this module is the correct runtime equivalent.
export {}
